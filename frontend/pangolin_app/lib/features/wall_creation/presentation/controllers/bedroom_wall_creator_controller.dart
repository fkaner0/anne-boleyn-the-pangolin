import '../../domain/virtual_canvas.dart';

class BedroomWallCreatorController {
  final VirtualCanvas canvas;

  BedroomWallCreatorController({VirtualCanvas? canvas})
    : canvas = canvas ?? const VirtualCanvas();

  void addTextBox() {}

  void addImage() {}

  void addSticker() {}
}
